^{:nextjournal.clerk/visibility {:code :hide}}
(ns hugsql.install
  (:require [nextjournal.clerk :as clerk]))

;; # 2. 安装
;; deps.edn 已添加: com.layerware/hugsql, hugsql-adapter-next-jdbc, com.github.seancorfield/next.jdbc, com.h2database/h2。
;; 执行 `clojure -P` 拉取依赖后即可使用。
